export default {
  // Methods we allow
  methods: "GET, POST, PUT, DELETE, OPTIONS, HEAD",
  // Allows all headers
  headers: "*",
  // Allow requests from all domains
  origins: "*", // Allows all origins
};
